[project]
name = "ann"
version = "0.1.0"
description = "Approximate Nearest Neighbor engine for toroidal space and fractional Minkowski distances"
authors = [
    {name = "Mário Antunes", email = "mario.antunes@example.com"}
]
dependencies = [
    "numpy",
]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
